// Membuat looping dengan logika do while dan for

const nama = "Haikel"; // Set tipe data String Haikel
const syarat = 100; // Set tipe data number 100
do { // Lakukan perulangan dengan kondisi;

if ((syarat / 200) > -1) { // Ketika variabel syarat itu lebih dari (>) -1. maka jalankan fungsi for
  for (let i = 0; i < 0; i++) { // Untuk i yang ditetapkan adalah 0, i lebih dari atau sama dengan(>=) 0, nilai i selalu meningkat 1(i++), maka
    console.log(i, nama); // Cetak i dan variabel nama.
  }
  }
} while (1 < 2);
